<?xml version="1.0" encoding="UTF-8"?>
<!--
  지도 기반 단일 화면이라 색인할 주소가 하나다. 단지를 고르거나 계산해도
  주소가 바뀌지 않으므로 넣을 URL 이 더 없다.

  `changefreq` 와 `priority` 는 넣지 않는다 — 구글이 무시한다고 밝힌 항목이다.
  `lastmod` 는 내용이 실제로 바뀔 때만 손으로 고친다. 배포할 때마다 올리면
  크롤러가 그 값을 믿지 않게 된다.
-->
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
  <url>
    <loc>https://tax.rich-group.kr/</loc>
    <lastmod>2026-08-11</lastmod>
  </url>
</urlset>
