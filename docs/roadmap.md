# 구축 로드맵

엔지니어링 규칙은 [AGENTS.md](../AGENTS.md)에 있다. 이 문서는 **무엇을 어떤 순서로
만드는지**만 정의한다.

각 Phase는 여러 Codex 태스크로 쪼개 디스패치한다. Phase 전체를 한 태스크로 주지 않는다.

---

## 의존 관계

```
P0 룰 엔진 ──────────────────────┐
                                 ├─→ P4 보유세 ─┐
P1 데이터 파이프라인 → P2 지도 ───┤              ├─→ P6 시뮬레이션 → P7 잔여 세제 → P8 상가·토지
                                 │              │
P3 공시가격 ─────────────────────┴─→ P5 양도세 ─┘
```

**병렬 가능한 묶음 (Wave)**

| Wave | Phase | 비고 |
|---|---|---|
| 1 | **P0, P1, P3** | 서로 독립. 동시에 착수 가능 |
| 2 | **P2** | P1 필요 |
| 3 | **P4, P5** | 서로 독립 |
| 4 | **P6** | P4·P5 필요 |
| 5 | **P7** | |
| 6 | **P8** | 별도 판단 후 착수 |

P3은 Worker 계층까지 Wave 1에서 만들 수 있다. UI 통합만 P2 이후로 미룬다.

**P0~P7이 주택 전용이다.** P8(상가·토지)은 예정만 되어 있고 착수 시점은 미정이다.
그래도 P0~P7이 주택 전용 가정을 코드에 박지 않도록 AGENTS.md §4에 자산 유형 규약을 두었다.

---

## P0 — 세법 룰 엔진

**목표** 외부 데이터에 의존하지 않는 순수 계산 계층. 이 프로젝트의 심장.

**산출물**
- `src/rules/` — 연도별 룰 테이블 (`2026`=현행, `2027`, `2028`, `2029`)
  - 재산세: 공정시장가액비율, 세율표, 지방교육세, 도시지역분
  - 종부세: 과세대상 기준, 기본공제, 공정시장가액비율, 세율표, 재산세 공제, 농특세
  - 양도세: 기본세율, 단기 중과세율, 장기보유특별공제, 기본공제
- `src/rules/rounding.ts` — 공용 반올림 함수 (R5: 단 하나만 존재)
- `src/holding/calc.ts` — 보유세 계산 (단일 주택 + 합산)
- `src/transfer/calc.ts` — 양도세 계산
- `tests/` — 골든 케이스 테스트

**검증**
- `docs/golden-cases.md`의 기대값과 **1원 단위까지** 일치
- 경계값 전부 포함: 9억 / 12억 / 14억 / 25억 / 30억, 보유 2·3·10·15년, 거주 2·10년
- 모든 세법 값에 개편안 페이지 출처 주석

**선행** 없음. 단, `docs/tax-rules-spec.md`와 `docs/golden-cases.md`가 먼저 있어야 한다.

---

## P1 — 데이터 파이프라인 (단지 마스터 + 실거래가)

**목표** 지도가 읽을 D1을 채운다. **이 UX의 관문.**

지도 라벨에 실거래가를 띄우기로 했으므로, 실거래가도 여기서 캐시한다.
**실시간 호출은 불가능하다** — data.go.kr은 `법정동코드 + 거래년월` 단위로만 응답해서,
지도 영역에 단지가 50개면 법정동 여러 개 × 여러 달을 호출해야 한다. 지도를 움직일 때마다
그럴 수 없다. 배치로 수집해 단지코드에 매칭해 저장하고, **지도는 D1만 읽는다.**

**산출물**
- `migrations/` — D1 스키마
  - `complex` — 단지코드 PK, 단지명, 법정동·도로명주소, 법정동코드, 사용승인일,
    동수, 세대수, lat, lng. **lat/lng 인덱스 필수**
  - `trade` — 단지코드 FK, 거래일, 거래금액, 전용면적, 층. 단지코드 + 거래일 인덱스
- `scripts/ingest-complexes.ts` — 국토부 단지 목록·기본정보 수집
- `scripts/geocode.ts` — 주소 → 좌표 일괄 변환 (카카오 로컬 API, **일회성 배치**)
- `scripts/ingest-trades.ts` — data.go.kr 실거래가 수집 + **단지 매칭**
- `worker/complex/` — bbox 조회 API. 단지 + 최근 거래 대표값을 함께 반환
- Cron — 실거래가 정기 갱신

**검증**
- 적재가 **원자적**이고 멱등하다 (R4-a). 검증 미달 시 기존 데이터 유지
- 단지 매칭률을 로그로 남긴다. 매칭 실패 건이 조용히 사라지지 않는다
- bbox 조회가 마커 상한을 넘지 않는다
- 지오코딩 실패 건이 전체 실패로 번지지 않는다

**선행** 없음.

**주의**
- 지오코딩은 배치 전용. 런타임 호출은 반려
- **실거래가 ↔ 단지 매칭이 이 단계의 최대 난관**이다. data.go.kr은 아파트명·지번·법정동을
  주고 단지 마스터는 단지코드·단지명·주소를 갖는데, 이름이 정확히 일치하지 않는다
  (`래미안` vs `래미안아파트`). `iros-property-lookup`의 `worker/trade/`에 이미
  `matchTrade` / `normalizeJibun` / `matchesDong`과 `matchLevel: 'lot' | 'candidate'`
  구분이 있으니 활용한다
- 실거래가에는 **해제된 거래**가 포함된다. `removeCanceled` 처리를 반드시 이식한다

---

## P2 — 지도 + 사이드바

**목표** 지도를 띄우고, 실거래가 라벨을 얹고, 클릭하면 사이드바가 열리는 데까지.

**산출물**
- `src/map/` — 카카오맵 SDK 로딩, bbox 기반 라벨 로딩, 줌 레벨 정책, 겹침 처리
- `src/map/PriceLabel` — `kakao.maps.CustomOverlay` 기반 2단 라벨 (가격 / 거래연도)
- `src/map/constants.ts` — 최소 표시 줌 레벨, 마커 상한 (R2)
- `src/sidebar/` — 단지 기본정보 + 실거래가 내역 + 연도별 평균가 차트

**검증**
- 전국 줌에서 라벨을 전부 뿌리지 않는다. 일정 줌 이상에서만 표시
- 지도 이동·확대 시 요청이 디바운스된다
- 라벨이 서로 겹쳐 읽을 수 없는 상태가 되지 않는다
- SDK 키가 소스에 박혀 있지 않다 (R2)

**선행** P1

**주의**
- `CustomOverlay`는 **클러스터링이 자동으로 되지 않는다.** 카카오의 `MarkerClusterer`는
  `Marker` 전용이라 적용되지 않는다. 줌 정책·상한·겹침 회피를 직접 구현해야 한다
- 차트는 React용 라이브러리를 쓴다 (`vue-chartjs` 아님)

---

## P3 — 주소→PNU + 공시가격

**목표** 단지·동·호를 특정해 공시가격을 가져온다.

**산출물**
- `worker/ldong/` — 법정동코드 KV 캐시, 주소→PNU 오프라인 변환
  (`iros-property-lookup`에서 이식)
- `worker/realty-price/` — 공동주택가격·개별주택가격 조회 (이식)
- `src/sidebar/UnitPicker` — **동/호 드롭다운** (P2 이후 통합)

**검증**
- 같은 단지에 대해 전년·당년 공시가격을 모두 가져온다
- 조회 실패와 "자료 없음"이 구분된다 (R4-b)
- 레이트리밋을 지킨다

**선행** Worker 계층은 없음. UI 통합은 P2.

**주의**
- **공시가격은 동/호를 특정해야만 조회된다.** realtyprice.kr이
  `searchApt.search`(동 목록) → `searchApt.search`(호 목록) →
  `townPriceListPastYearMap.search`(가격) 순으로 타고 들어가는 구조다.
  동/호 없이 단지 대표 공시가격을 얻을 방법은 없다 — 층·향·면적마다 다르기 때문이다
- 동/호 **목록을 API가 주므로 드롭다운으로 제공**한다. 사용자가 타이핑하게 하지 않는다
- **개별주택(단독·다가구)은 동/호가 없다.** 필지 단위라 주소만으로 조회된다.
  "공동주택은 동/호 필수, 개별주택은 주소만"
- `townPriceListPastYearMap.search`가 이름·반환형태상 **과거 연도 가격을 함께 주는 것으로
  보인다.** 사실이면 연도 파라미터화가 불필요해진다. **실제 응답을 찍어 먼저 확인할 것**

---

## P4 — 내 부동산 목록 + 보유세

**목표** 지도에서 담은 물건들을 합산해 보유세를 계산한다.

**산출물**
- `src/portfolio/` — "내 부동산으로 추가" 목록. 클라이언트 상태 (localStorage, 서버 저장 없음)
  - 공동명의 지분율, 거주 주택 지정, 조정대상지역 여부
  - 항목은 `assetKind` 필드를 갖는다. 현재 값은 주택 계열만 (AGENTS.md §4)
- `data/adjusted-areas.ts` — 조정대상지역 목록 (국토부 고시 기반, 갱신 절차 문서화)
- `src/holding/` — 다주택 합산, 지분 안분, 현행 vs '27 vs '28 비교 화면

**P0에서 넘어온 제약 — 여기서 해소한다**

P0-2의 `assertValidHoldingTaxInput`이 `householdHomeCount === items.length`를 강제한다.
즉 **포트폴리오가 세대의 모든 주택을 담고 있다고 가정**한다.

실제로는 다를 수 있다. 배우자가 단독소유한 주택은 내 종부세 합산에는 안 들어가지만
(인별 과세), **재산세 1세대1주택 특례와 종부세 1세대1주택자 판정에는 영향을 준다**
(세대 2주택). 현재 구현으로는 이 상태를 표현할 수 없고 `RangeError`가 난다.

틀린 세액이 나오는 게 아니라 **명시적으로 실패**하므로 P0 시점에서는 문제가 아니다.
P4에서 "내 부동산 목록에 무엇을 담는가"를 정하면서 함께 해소한다.

| 선택 | 결과 |
|---|---|
| 목록에 세대 전체 주택을 담는다 | 현재 제약 유지 가능. 단 배우자 물건도 입력해야 함 |
| 목록은 본인 물건만 담는다 | 제약을 풀고 `householdHomeCount`를 독립 입력으로 |

**검증**
- 다주택 합산이 골든 케이스와 일치
- 종부세 기본공제 `4억 + 5억 × (거주주택 ÷ 합계)`가 정확히 적용
- 일부 주택의 공시가격이 없으면 **계산하지 않고** 어느 건이 왜 없는지 알린다 (R4-b)
- 과세표준·적용세율·공제액 등 중간값이 함께 표시된다

**선행** P0, P2, P3

---

## P5 — 양도세

**목표** 내 주택 목록에서 한 채를 골라 양도세를 계산한다.

**산출물**
- `src/transfer/` — 양도세 계산 화면
  - 취득가액·필요경비·보유기간·거주기간은 사용자 입력 (어떤 API로도 안 나옴)
  - 양도가액은 P1의 실거래가를 참고값으로 제시

**검증**
- 1세대1주택 비과세 판정에 **거주요건**이 반영된다
  (기존 서비스가 놓친 지점 — 조정대상지역 취득 주택은 2년 거주 필요)
- 장기보유특별공제 한도(20억/10억) 적용 시, 화면에 표시되는 공제율이
  **한도 적용 후 실효율**이다 (기존 서비스는 적용 전 값을 표시하는 오류)
- 양도세 기본공제 2,500만원이 **'27년 양도분부터** 적용된다 (개편안 p68)

**선행** P0, P3

---

## P6 — 매도 시점 시뮬레이션

**목표** "팔까 보유할까"에 답한다. 매도 시점별 총 세부담(양도세 + 누적 보유세) 비교.

**산출물**
- `src/simulate/` — '26 / '27 / '28 / '29 매도 시나리오 비교

**검증**
- 공시가 상승 시나리오를 **보유세와 양도세 양쪽에 동일하게** 적용한다.
  한쪽만 동결로 두면 보유 비용이 과소평가된다 (기존 서비스의 오류)
- 가정(공시가 변동률, 매도 시점, 보유세 산입 범위)이 화면에 명시된다

**선행** P4, P5

---

## P7 — 잔여 세제

**목표** 여기까지 하면 기존 서비스보다 정확해진다.

**산출물**
- 세부담 상한 150% → 200% (개편안 p65)
- 고령자·거주기간 세액공제 개편, 금액 한도 '27년 800만 / '28년~ 600만 (p64)
- 다주택 조정대상지역 중과 한시완화 (p72)
- 고령 1주택자 한시 감면, 조특법 §71의3 (p69)

**선행** P6

---

## P8 — 상가·토지 (예정, 착수 시점 미정)

**목표** 주택 외 자산으로 범위를 넓힌다. P0~P7 완료 후 별도 판단.

### 왜 별도 Phase인가

주택과 **세목 자체가 다르다.** 기존 계산에 자산 유형만 추가하면 되는 일이 아니다.

| | 주택 (P0~P7) | 상가·토지 (P8) |
|---|---|---|
| 재산세 과세 기준 | 주택 공시가격 | 건물분 시가표준액 + 토지분 개별공시지가 |
| 종부세 | 주택분 (합산) | 건축물은 **대상 아님**. 토지분은 별도 체계 |
| 양도세 장특공제 | 장기**거주**소득공제 | 장기**보유**소득공제 (개편안 p56 이원화) |
| 비과세 | 1세대1주택 12억 | 해당 개념 없음 |

**토지 3분류 판정**(종합합산 / 별도합산 / 분리과세)이 새로 들어온다. 실무에서 가장
까다로운 부분이며, 이 판정을 틀리면 세액이 통째로 어긋난다.

### 개편안이 토지에 대해 정한 것

| 항목 | 내용 | 시행 |
|---|---|---|
| 비사업용 토지 장특공제 (p95 ①) | **적용 배제** — 배제자산에 추가 | '28.1.1. 양도분~ |
| 비사업용 토지 양도세 (p95 ②) | 기본세율 +10%p → **+20%p** | '28.1.1. 양도분~ |
| 종합합산 토지 종부세 (p96 ④) | 45억 초과 구간 3% → **4%**<br>(15억 이하 1% / 15~45억 2%는 유지) | '28.1.1. 성립분~ |
| **별도합산 토지** (상가·공장·물류시설 부지) | **개편 없음 (좌동)** | — |

> ⚠️ **p96의 별도합산 세율표는 PDF 텍스트 추출이 깨져 있다.**
> ("200억 이하 0.5% / ~~15억 초과 45억 이하 0.6%~~ / 400억 초과 0.7%" — 가운데 행이 명백한 오류)
> 구현 전 **원문을 눈으로 확인**하고 `tax-rules-spec.md`에 정확히 옮길 것.

> **상가는 개편 효과가 거의 없다.** 부속토지가 별도합산이고 그 세율은 개편 대상이 아니며,
> 건축물분 재산세도 개편이 없다. "개편 전후 비교"라는 이 서비스의 핵심 가치가
> 상가에서는 나오지 않는다는 뜻이다. 착수 판단 시 고려할 것.

### UX 문제 — 지도로는 안 된다

아파트는 단지 하나에 마커 하나지만 **토지는 필지 단위라 전국 수천만 필지**다.
지도에 뿌릴 수 없다. 상가·토지는 **주소 직접 입력**이라는 별도 진입 경로가 필요하다.

### 데이터

| 자산 | 원천 | 상태 |
|---|---|---|
| 상가·오피스텔 기준시가 | 국세청 홈택스 | `iros-property-lookup`에 구현 있음. 비공식·HMAC 서명 |
| 개별공시지가 | 국토부(온나라) → LH 프록시 | `iros-property-lookup/worker/landinfo/`에 구현 있음 |
| 건물분 시가표준액 | **원천 미확인** | 지자체 소관. 조사 필요 |

> 홈택스 기준시가는 **양도세·상속증여세 평가액**이지 보유세 과세표준이 아니다
> ([external-apis.md §7](./external-apis.md)). 보유세에 그대로 쓰면 틀린다.

**선행** P7

---

## 선행 문서 (P0 착수 전 필요)

| 문서 | 용도 | 상태 |
|---|---|---|
| [`docs/golden-cases.md`](./golden-cases.md) | 기대값. 없으면 검증 자체가 불가능 | ✅ 완료 |
| [`docs/tax-rules-spec.md`](./tax-rules-spec.md) | 개편안 값 전량 전사 + 룰 데이터 스키마 | ✅ 완료 |
| [`docs/external-apis.md`](./external-apis.md) | 비공식 엔드포인트 계약·실패 모드 | ✅ 완료 |

**선행 문서 완료.** P0·P1·P3 전부 착수 가능하다.
단, `external-apis.md` §9의 미확인 5건은 P1·P3 착수 시 **실제 응답을 먼저 확인**하고
문서를 갱신한 뒤 스키마를 확정한다.

---

## 확정된 결정

- **주변 편의시설 검색 — 넣지 않는다.** 세금 계산과 무관하고, 기존 서비스처럼
  지도 `idle` 이벤트마다 호출하면 카테고리 장소 검색(2원/건)이 지도 SDK(0.1원/건)보다
  먼저 쿼터를 소진한다
- **지도 라벨 — 실거래가.** 그래서 실거래가 캐시가 P1로 앞당겨졌다
- **디자인 — WhereIsMyHome은 시각적 레퍼런스일 뿐 코드를 복사하지 않는다.**
  라이선스가 없어 HTML·CSS도 저작권 대상이다. 레이아웃 개념은 참고 가능하지만
  마크업·CSS·클래스명·자산은 복사 금지. Bootstrap 4 + bootstrap-vue 2(Vue 2 전용)라
  React에서 동작하지도 않는다. 스크린샷을 보고 새로 만든다

---

## 미결 사항

착수 전 사용자 확인이 필요하다.

1. ~~**카카오 무료 쿼터**~~ — **확정: 무료 쿼터 적용 대상.**
   개발자 계정의 첫 활성화 앱이다. SDK 30만건/일, 좌표 변환 10만건/일, 전체 300만건/월.
   비즈월렛 연결 없이 시작할 수 있다. 다만 **한도가 무제한은 아니므로**
   지오코딩은 배치 전용, 장소 검색은 미사용이라는 제약은 그대로 유지한다
2. ~~**라이선스**~~ — **확정: MIT** ([LICENSE](../LICENSE)).
   `iros-property-lookup`은 GPL-3.0이지만 **기여자가 저작권자 본인 한 명**이므로
   가져오는 코드를 MIT로 재라이선싱할 권한이 있다.
   **P1·P3에서 수집기를 이식할 때 원본이 GPL이라는 이유로 이 저장소를 GPL로 바꾸지 않는다.**
   제3자 GPL 코드는 그대로 가져오지 말 것 — 발견하면 `ask`한다
3. **면책 문구** — 개편안은 **국회 통과 전 정부안**이다. 화면 표기 방식을 정한다
