# incometax — 부동산 보유세·양도세 계산기

이 파일은 **이 저장소의 엔지니어링 규칙에 대한 유일한 출처(SSOT)** 다.
Codex·Claude·리뷰어 등 모든 에이전트가 이 파일을 읽는다. 다른 문서는 이 파일을
참조만 하고, 여기 있는 규칙을 복사하지 않는다.

---

## 1. 프로젝트

2026년 세제개편안을 반영한 **지도 기반 부동산 보유세·양도세 계산기**.

지도에서 아파트 단지를 찾아 클릭하면 사이드바가 열리고, 거기서 동/호를 골라
**내 부동산 목록에 담는다.** 담긴 물건 전체를 합산해 보유세를 계산하고,
하나를 골라 양도세와 매도 시점별 총 세부담을 비교한다.

```
지도 (마커 클릭)
  └→ 사이드바: 단지 기본정보 → 동/호 선택 → 공시가격 → [내 부동산으로 추가]
        ↓
     내 부동산 목록 (누적)
       ├ 전체 합산 → 보유세 (현행 vs '27 vs '28)
       └ 하나 선택 → 양도세 · 매도 시점 시뮬레이션
```

**지도는 입력 수단이고, 계산의 단위는 "내 부동산 목록"이다.** 마커 클릭에서 계산으로
곧장 이어지지 않는다. 종부세 기본공제가 `4억 + 5억 × (거주주택 공시가격 ÷ 전체 합계)`라
다주택자는 전체를 모아야만 계산이 성립하기 때문이다.

> **"주택"이 아니라 "부동산"인 이유.** 현재 구현 범위는 주택뿐이지만, 상가·토지가
> P8로 예정되어 있다. 자산 유형을 이름과 타입에 박아두지 않는다 (§4 자산 유형 참조).

**구축 순서는 [docs/roadmap.md](./docs/roadmap.md)를 따른다.** 선행 단계가 완료되기 전에
후행 단계에 착수하지 않는다.

**범위 밖 (구현 금지)**
로그인·회원·인증, 게시판·공지사항, 뉴스, 커뮤니티, **주변 편의시설 검색**.
사용자 상태는 전부 클라이언트에 둔다. 서버는 사용자별 데이터를 저장하지 않는다.
"내 부동산 목록"도 서버에 저장하지 않는다.

**디자인 출처.** WhereIsMyHome은 **시각적 레퍼런스일 뿐 코드를 복사하지 않는다.**
라이선스가 없어 HTML·CSS도 저작권 대상이다. 레이아웃 개념(플로팅 검색 패널, 2단 가격
라벨, 클릭 시 사이드바)은 참고해도 되지만 마크업·CSS 규칙·클래스명·이미지 자산은
복사 금지. 어차피 Bootstrap 4 + bootstrap-vue 2(Vue 2 전용)라 React에서 동작하지 않는다.

---

## 2. 스택

| 영역 | 선택 |
|---|---|
| 프론트 | React + TypeScript + Vite |
| 지도 | 카카오맵 JavaScript SDK |
| 백엔드 | Cloudflare Workers (TypeScript) |
| 단지 마스터 | Cloudflare **D1** (좌표 범위 조회) |
| 법정동코드 캐시 | Cloudflare **KV** |
| 배포 | Cloudflare (wrangler) |

**Pages가 아니라 Workers인 이유.** **Cron Triggers는 Workers에만 있고 Pages Functions에는
없다.** 이 프로젝트는 법정동코드 캐시 일일 갱신과 실거래가 정기 갱신에 Cron이 필수라
Workers가 유일한 선택이다. 배포 형태는 **Workers with static assets** —
Vite가 `dist/`를 만들고 Worker가 그것을 서빙하면서 `/api/*`를 처리한다.
편의를 이유로 Pages로 바꾸지 않는다.

**저장소를 둘로 나눈 이유.** 지도 영역(bbox) 안의 단지를 찾으려면 좌표 범위 질의가
필요한데 KV는 키-값 저장소라 prefix 나열만 가능하다. 단지 마스터는 D1(SQLite),
단순 조회인 법정동코드 캐시는 KV를 쓴다. **용도를 바꿔 쓰지 않는다.**

> P1에서 D1 바인딩이 들어오면 `@cloudflare/vite-plugin` 전환을 검토한다.
> 현재 `concurrently`로 Vite와 `wrangler dev`를 따로 띄우는 구성은 바인딩이 없어
> 문제가 없지만, D1이 붙으면 로컬 테스트가 번거로워진다.

R2는 쓰지 않는다. 그 밖의 새 인프라 리소스가 필요하다고 판단되면 **먼저 코디네이터에게
`ask`로 확인**한다.

### 카카오맵 SDK 주의사항

- **JavaScript 키**를 쓴다. REST API 키를 넣으면 에러가 난다
- JavaScript 키는 **클라이언트에 노출되므로 시크릿이 아니다.** 유일한 방어는 도메인
  등록이다. 배포 도메인과 `localhost`를 모두 콘솔에 등록한다
- 무료 쿼터는 **개발자 계정 기준 첫 번째 활성화 앱에만** 제공된다 (SDK 30만건/일,
  좌표 변환 10만건/일, 전체 300만건/월). 초과 시 429
- 지오코딩은 **런타임이 아니라 일회성 배치**로만 호출한다. 사용자 요청마다 좌표를
  변환하지 않는다 — 단지 좌표는 D1에 저장된 값을 쓴다
- **런타임 지도-이벤트 연동 장소 검색은 쓰지 않는다.** 지도 `idle`·`dragend` 이벤트에
  `kakao.maps.services.Places`를 묶지 않는다. 기존 서비스처럼 하면 카테고리 장소
  검색(2원/건)이 지도 SDK(0.1원/건)보다 먼저 쿼터를 소진한다. 주변 편의시설은 범위 밖이다
- **예외 1 — 클라이언트 SDK 검색창 호출은 허용한다.** D1에 없는 연립·다세대를
  찾으려면 런타임 카카오 호출이 필수다. `Places.keywordSearch`를 **사용자가
  입력한 검색어에 대해 디바운스(250ms) 후 1회** 호출하는 것은 허용한다.
  `Geocoder.addressSearch`는 카테고리 정보가 없어 비주택이 노출되므로 사용하지
  않는다. 지도 이벤트에 연동하거나, 사용자 입력 없이 자동으로 호출하는 것은
  여전히 금지다. 키워드 검색 결과는 주거시설 카테고리(`아파트`, `빌라,주택`,
  `도시형생활주택`)만 남긴다
- **예외 2 — 배치의 REST 키워드 검색은 허용한다.** 단지 마스터를 만들 때
  `dapi.kakao.com/v2/local/search/keyword.json`을 **배치에서** 호출해 주소와 좌표를
  확보한다. 국토부 기본정보 API의 일일 한도(10,000)가 필요량(22,259)에 못 미쳐
  이 경로가 유일하게 성립한다
- 지도 라벨에는 **실거래가**를 띄운다. 공시가격·세액은 동/호 단위라 단지 대표값이
  없으므로 라벨에 띄울 수 없다 (사이드바에서 동/호를 고른 뒤 계산한다)

---

## 3. 절대 규칙

위반은 리뷰 반려 사유다. 리뷰어는 이 절의 순서대로 기계적으로 검증한다.

### R5. SSOT — 최우선

> 하나의 사실은 코드베이스에 **정확히 한 번만** 존재한다.

**반면교사.** 기존 서비스(propertytax.co.kr)는 종부세 세율표를 서버(보유세)와
클라이언트(양도세)에 **이중으로** 두었다. 그 결과 양도세 쪽에만 적용시기 오류
(개편안은 '27년 양도분부터인데 코드는 '28년부터)가 남아 두 도구가 서로 다른 답을 냈다.
같은 실수를 반복하지 않는다.

구체적으로:

- **세율·공제·한도는 `src/rules/` 한 곳에만 존재**한다. 보유세와 양도세가 같은
  테이블을 참조한다. 어느 한쪽에 숫자를 다시 적으면 반려.
- **타입은 `shared/`에만 정의**한다. Worker와 프론트가 같은 타입을 import 한다.
  같은 모양의 인터페이스를 양쪽에 각각 선언하면 반려.
- **계산 로직은 한 곳**. 프론트에서 표시용으로 세액을 다시 계산하지 않는다.
  표시가 필요하면 계산 결과를 전달받아 포맷만 한다.
- **문서도 대상**이다. 엔지니어링 규칙은 이 파일에만 있다.

새 값을 적기 전에 항상 먼저 묻는다: **"이 사실이 이미 어딘가에 있는가?"**

### R1. 중첩 조건문 객체화

세법은 조건 분기(주택수 × 거주여부 × 연도 × 지역)가 본질이라 순진하게 짜면
if가 4단으로 쌓인다. 조건 조합을 **키로 만들어 테이블을 조회**하는 형태로 바꾼다.

```ts
// 금지 — 중첩 if로 세법 분기를 표현
if (isOneHouse) {
  if (isResiding) { deduction = 1_400_000_000 }
  else { deduction = 900_000_000 }
} else { ... }

// 요구 — 조건을 키로, 값은 테이블에서
const DEDUCTION = {
  'oneHouse:residing':    1_400_000_000,
  'oneHouse:nonResiding':   900_000_000,
} as const
const deduction = DEDUCTION[`${houseKind}:${residency}`]
```

- **금지**: 2단계를 넘는 중첩 if, 세법 분기를 if/else 체인으로 표현
- **허용**: 가드절(early return)은 중첩이 아니다. 단일 if/else도 그대로 둔다
- **주의**: 규칙을 문자 그대로 적용해 단순한 2분기까지 테이블로 만들지 않는다.
  판단이 서지 않으면 `ask`로 확인한다

### R2. 하드코딩 금지

하드코딩은 **"값이 바뀔 때 코드를 고쳐야 하는 것"** 을 뜻한다. 대상별로 다르게 처리한다.

| 종류 | 처리 | 위치 |
|---|---|---|
| 세율·공제·한도 | **버전된 룰 데이터** | `src/rules/<연도>.ts` |
| 외부 URL·엔드포인트 | 상수 모듈 | `worker/config/` |
| 서버 API 키·계정 | Cloudflare Secret | `wrangler secret` |
| 카카오 JavaScript 키 | 빌드 환경변수 (`VITE_*`) | `.env` / CI 변수 |
| 지도 줌 레벨·마커 상한 | 이름 붙인 상수 | `src/map/constants.ts` |
| 타임아웃·페이지크기·재시도 | 이름 붙인 상수 | 해당 모듈 최상단 |
| UI 문자열 | 상수 또는 i18n | `src/messages/` |

카카오 JavaScript 키는 클라이언트 번들에 포함되므로 Secret으로 취급하지 않는다.
다만 **소스에 직접 박지 않는다** — 환경별로 다른 키를 쓸 수 있어야 한다.

세율 숫자를 **환경변수로 빼지 않는다.** 법으로 고정된 값이지 배포 환경별 설정이 아니다.
연도별 룰 데이터로 관리하는 것이 올바른 해소다.

매직 넘버·매직 문자열은 전부 이름을 붙인다. `0.7`이 아니라 `FAIR_MARKET_RATIO_2027`.

### R3. 모듈화

- 한 파일은 **한 가지 책임**만 가진다
- 계산(순수 함수) / 수집(I/O) / 표현(UI)을 **파일 단위로 분리**한다. 순수 계산 함수는
  `fetch`·`Date.now()`·환경변수를 참조하지 않는다 — 인자로 받는다
- 파일이 300줄을 넘으면 분리를 검토한다. 절대 기준은 아니지만 신호로 삼는다
- 복사·붙여넣기 금지. 두 곳에 같은 코드가 생기면 그 시점에 공통화한다 (R5와 같은 원칙)

### R4. 트랜잭션 / 부분 실패

사용자 요청 경로에는 쓰기가 없다(로그인 없음, 내 주택 목록은 클라이언트 보관).
트랜잭션 문제는 **두 지점에서만** 발생한다. 그 밖에서 트랜잭션을 찾지 말 것.

**(a) 단지 마스터 일괄 적재 — 진짜 트랜잭션 대상**

전국 단지 수만 건을 D1에 적재·갱신할 때 중간에 실패하면 지도가 깨진다.

- 갱신은 **원자적**이어야 한다. 부분 적재된 상태가 사용자에게 노출되면 안 된다
- **검증을 통과한 경우에만 교체**한다. 최소 기대 건수와 필수 지역 표본을 확인하고,
  미달이면 기존 데이터를 유지한다. 갱신 실패가 서비스 중단이 되어서는 안 된다
- 배치는 재실행 가능해야 한다(멱등). 중복 실행이 데이터를 망가뜨리지 않는다

**(b) 외부 API 다건 조회 — 부분 실패**

주택 3채의 공시가격을 조회할 때 2채만 성공하면 어떻게 할 것인가.

- 다건 조회는 **건별로 성공/실패를 분리**해 반환한다. 한 건이 실패해도 전체를 버리지 않는다
- 단, **세액 계산은 부분 데이터로 수행하지 않는다.** 다주택 합산은 전부 있어야 성립한다.
  일부가 없으면 계산을 중단하고 어느 건이 왜 없는지 사용자에게 알린다
- 조회 실패와 "해당 자료 없음"을 구분한다. 전자는 재시도 대상, 후자는 정상 결과다
- 외부 API에는 타임아웃과 재시도 상한을 반드시 둔다. 무한 대기 금지

---

## 4. 도메인 규약

### 금액

- 단위는 **원(KRW)**, 타입은 정수. 만원 단위를 내부 계산에 쓰지 않는다
  (입력·표시에서만 변환)
- 계산 중간값에 부동소수점이 개입하는 지점(비율 곱)은 **마지막에 한 번만** 반올림한다.
  단계마다 반올림하면 골든 케이스와 어긋난다
- 반올림 함수는 `src/rules/`의 공용 함수 하나만 쓴다 (R5)

### 자산 유형

현재 구현 범위는 **주택뿐**이다. 상가·토지는 P8로 예정되어 있다.

지금 토지·건축물 세제를 구현하지 말 것. 다만 **주택 전용 가정을 이름과 구조에 박지 않는다.**

```ts
type AssetKind = 'apartment' | 'detachedHouse'   // 현재 지원 (주택)
//             | 'commercial'                     // 상가·오피스텔 (건축물) — P8
//             | 'land'                           // 토지 — P8

interface PortfolioItem { assetKind: AssetKind; /* ... */ }
```

**토지는 `land` 하나로 부족하다.** 재산세·종부세 세율을 가르는 건 자산 유형이 아니라
과세 구분이다. P8에서 별도 축으로 들어온다 — **지금 만들지 않는다.**

```ts
// P8. 지금 구현하지 말 것 — 축이 둘이라는 사실만 기록한다
type LandTaxCategory = 'aggregate' | 'separateAggregate' | 'separate'
//                     종합합산      별도합산              분리과세
```

**농지·자경농지를 `AssetKind`에 넣지 않는다.** 재산세에서는 분리과세 토지의 세율
구분(0.07%)이고, 자경 여부는 양도세 감면 요건이다. 성격이 다른 두 축을 하나의
열거형에 섞는 것이 곧 R1 위반이다.

| 하라 | 하지 마라 |
|---|---|
| `PortfolioItem`, `assetKind` 필드 | `MyHouse`, `houses[]` 같은 주택 전용 명명 |
| 세율 조회를 `세목 × 자산유형`으로 | 주택 세율을 세목 함수에 직접 박기 |
| 미지원 자산 유형은 명시적으로 거부 | 조용히 주택으로 처리 |

**과잉 추상화는 금지한다.** 토지용 세율 테이블·판정 로직을 미리 만들지 않는다.
지금 필요한 건 "나중에 자산 유형이 늘어도 기존 코드를 갈아엎지 않는 정도"이지,
쓰지 않는 확장점을 미리 파는 것이 아니다. 판단이 서지 않으면 `ask`한다.

### 좌표

- 좌표계는 **WGS84** (카카오맵 기본). 다른 좌표계를 섞지 않는다
- 좌표는 **단지 식별용 표시 데이터**일 뿐, 세액 계산에 절대 관여하지 않는다.
  계산 경로는 주소·법정동코드·PNU만 쓴다. 좌표를 계산 입력으로 넘기면 반려
- 단지 좌표는 D1에 저장된 값을 읽는다. 런타임 지오코딩 금지 (R2 참조)

### 지도 응답 페이로드

**지도 bbox 응답에는 화면을 그리는 데 필요한 필드만 담는다.** 한 화면에 수백 건이
실리므로, 안 쓰는 필드 하나가 곧바로 수백 배로 불어난다.

- 라벨 응답 = 식별자 + 표시 이름 + 좌표. 그 이상은 반려
- 상세는 단지를 열 때 별도 엔드포인트로 받는다
- 필드를 추가하려면 **그 필드를 지금 읽는 코드**를 함께 제시한다. "나중에 쓸 것 같아서"는 반려

> 호갱노노는 마커 하나에 단지 레코드 전체(44필드 + 51필드 `area` 객체)를 실어
> 한 화면 501.8 KB를 보낸다 — 단지당 3,425 B. 라벨에 필요한 건 이름과 좌표뿐인데도
> 그렇다. 측정치는 [docs/hogangnono-analysis.md §3.2](./docs/hogangnono-analysis.md).

### 세법 룰 데이터

`src/rules/` 아래 값에는 **출처를 주석으로 단다.** 근거 없는 숫자는 반려.

```ts
// 종부세 세율 — 2026 세제개편안 상세본 p63 (문서 p63 / PDF p76)
// '28.1.1. 이후 납세의무 성립분부터 적용, 주택수 기준 폐지·일원화
export const COMPREHENSIVE_TAX_BRACKETS_2028 = [...]
```

세법은 매년 바뀐다. 연도를 타입에 반영해 **새 연도 추가 시 기존 코드를 고치지 않도록**
설계한다.

### 계산 결과

세액만 반환하지 않는다. **과세표준·적용세율·공제액 등 중간값을 함께 반환**한다.
사용자가 근거를 확인할 수 있어야 하고, 테스트도 중간값 단위로 검증한다.

---

## 5. 검증

세금 계산은 **정답이 있는 도메인**이다. 테스트 없는 구현은 완료가 아니다.

- 룰 엔진의 모든 공개 함수는 테스트를 동반한다
- 경계값을 반드시 포함한다: 9억 / 12억 / 14억 / 25억 / 30억, 보유 2·3·10·15년, 거주 2·10년
- 골든 케이스(`docs/golden-cases.md`)의 기대값과 **1원 단위까지** 일치해야 한다
- 기대값을 구현에 맞추어 수정하지 않는다. 불일치하면 구현이 틀렸거나 골든 케이스가
  틀린 것이므로, **임의로 고치지 말고 `ask`로 확인**한다

---

## 6. 명령

```bash
npm install
npm run dev        # 프론트 + Worker 로컬
npm test           # 룰 엔진 테스트
npm run typecheck
npm run lint
npm run deploy     # Cloudflare 배포 — 사람이 직접 실행. 에이전트는 실행 금지
```

로컬 시크릿은 `.dev.vars`에 둔다. **키를 커밋하지 않는다.**

---

## 7. 완료 정의 (DoD)

작업을 `worker_done`으로 보고하기 전에 전부 충족해야 한다.

1. `npm test` / `npm run typecheck` / `npm run lint` 통과
2. 절대 규칙 R1~R5 자가 점검 완료
3. 새 계산 로직에 테스트 추가, 골든 케이스와 일치
4. 새로 추가한 세법 값에 출처 주석 존재
5. 커밋하지 않은 임시 파일·디버그 로그·주석 처리된 코드 없음

**보고 형식.** `worker_done`에 무엇을 바꿨는지, 무엇을 발견했는지, 무엇이 남았는지를
쓴다. 규칙 위반을 알면서 남겼다면 반드시 명시한다. 숨기면 리뷰에서 드러난다.

---

## 8. 판단이 필요할 때

추측해서 진행하지 않는다. 다음은 **`ask`로 코디네이터에게 확인**한다.

- 세법 해석이 갈리는 경우 (개편안 문언이 모호할 때)
- 골든 케이스와 구현이 불일치할 때
- 새 인프라 리소스(D1·R2·큐 등)가 필요해 보일 때
- 절대 규칙끼리 충돌하는 것처럼 보일 때
- 범위 밖 기능이 필요해 보일 때

```bash
orca orchestration ask --question "<질문>" --options "<선택지>" --timeout-ms 600000 --json
```

---

## 9. 중요한 전제

**2026년 세제개편안은 정부안이며 국회 통과 전이다.** 확정 세법이 아니다.

- 계산 결과에는 항상 "정부안 기준" 표시가 따라붙어야 한다
- 국회 심의 과정에서 내용이 바뀔 수 있으므로, 룰 데이터는 **개정 가능성을 전제로**
  버전을 나눈다
- 현행법과 개편안을 **동시에 계산해 비교**하는 것이 이 서비스의 핵심 가치다.
  개편안만 계산하지 않는다
